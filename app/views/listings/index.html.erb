<div class="jumbotron" >
 <h2 style="font-size: 42px;">あなたのスキルを出品してみませんか？</h2>
 <h3 style="font-size: 28px;">あなたの作品や知識を必要としてる人がきっと見つかるでしょう！</h3>  
</div>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-xs-12 col-sm-6 col-md-3">
          <div class="thumbnail"> 
            <% if listing.image? %>
              <%= link_to (image_tag listing.image.url), listing  %>
            <% end %>       
         
          <div class="caption">
          <h3><%= listing.name %></h3>
          <p class="textOverflow"><%= listing.description %></p>
          <p><%= "#{listing.user.name} 様が出品中" %></p>
          <% if (user_signed_in?) && (current_user == listing.user) %>
            <%= link_to '編集', edit_listing_path(listing) ,class:"btn btn-link"%>
            <%= link_to '削除', listing, method: :delete, data: { confirm: 'Are you sure?' }  ,class:"btn btn-link"%>     
          <% end %>
        </div>
       </div>        
      </div>
    <% end %>
  </div>
 
</div>

<% if user_signed_in? %>
  <%= link_to '新規商品追加', new_listing_path, class: "btn btn-success" %>
<% end %>


  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
         <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
         <script>
           $('.dropdown-toggle').dropdown()
  </script>